{
  "rules": {
    ".read": false,
    ".write": false,
    "blobio": {
      ".read": true,
      "arena": {
        "players": {
          "$playerId": {
            ".write": true,
            ".validate": "newData.val() === null || (newData.hasChildren(['n', 'c', 'a', 'm', 's', 't']) && newData.child('n').isString() && newData.child('n').val().length > 0 && newData.child('n').val().length <= 16)"
          }
        },
        "kills": {
          "$playerId": {
            ".write": true
          }
        },
        "eaten": {
          "$playerId": {
            ".write": true
          }
        }
      },
      "hall": {
        ".write": "auth != null && auth.token.firebase.sign_in_provider != 'anonymous'",
        "$i": {
          ".validate": "newData.hasChildren(['n', 's', 't', 'u']) && newData.child('n').isString() && newData.child('n').val().length > 0 && newData.child('n').val().length <= 16 && newData.child('s').isNumber() && newData.child('s').val() >= 0 && newData.child('s').val() <= 100000000 && newData.child('t').isNumber() && newData.child('u').isString() && newData.child('u').val().length > 0 && newData.child('u').val().length <= 128"
        }
      },
      "users": {
        "$uid": {
          ".write": "auth != null && auth.uid == $uid",
          ".validate": "newData.hasChildren(['best', 'nick', 't', 'level', 'glory']) && newData.child('best').isNumber() && newData.child('best').val() >= 0 && newData.child('nick').isString() && newData.child('nick').val().length > 0 && newData.child('nick').val().length <= 16 && newData.child('t').isNumber() && newData.child('level').isNumber() && newData.child('level').val() >= 1 && newData.child('level').val() <= 99 && newData.child('glory').isNumber() && newData.child('glory').val() >= 0"
        }
      }
    }
  }
}
